ref:
  as: ipi-conf-featureSet
  from_image:
   namespace: origin
   name: centos
   tag: '8'
  commands: ipi-conf-featureSet-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI manual-creds configure step updates install-config.yaml to set credentialsMode to Manual.
